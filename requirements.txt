# Dependências do automatizador de NFS-e Nacional
pandas>=2.2
openpyxl>=3.1          # engine de leitura do .xlsx usada pelo pandas
requests>=2.32         # cliente HTTP (a conexão mTLS é montada em nfse/certificado.py)
cryptography>=42.0     # leitura do certificado A1 (.pfx/.p12 e PEM)
lxml>=5.0              # XML + assinatura XMLDSig (montada manualmente em nfse/assinatura.py)
python-dotenv>=1.0     # carrega credenciais do arquivo .env (modo linha de comando)
keyring>=25.0          # guarda as senhas no cofre do sistema (modo interface gráfica)

# --- Interface gráfica (aplicativo instalado no laptop) ---
fastapi>=0.115         # servidor da interface, rodando só em 127.0.0.1
uvicorn>=0.32          # servidor ASGI que executa o FastAPI
jinja2>=3.1            # templates das telas
python-multipart>=0.0.9  # upload da planilha e do certificado pelo navegador
